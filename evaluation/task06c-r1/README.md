# Task 06C-R1 reliability correction

This directory is independent from the immutable Task 06 and Task 06C evidence.
Each live iteration has its own versioned directory and may be written only
once. Provider-free verification reads committed public-safe exports only.

Human reviewer packets are generated only after the technical gate passes.
Private state, coordinator mappings, reviewer identities, judgments, and
attestations remain under Git-ignored paths.
